CREATE TABLE ITEMS (ITEMNAME VARCHAR(255) PRIMARY KEY, WEIGHT INT, PRICE INT, QUANTITY INT); 
CREATE TABLE ORDERS (ORDERID INT PRIMARY KEY GENERATED ALWAYS AS IDENTITY,  USERNAME VARCHAR(255));
ALTER TABLE ORDERS ADD FOREIGN KEY (USERNAME) REFERENCES USERS(USERNAME);
ALTER TABLE ORDERS ADD COLUMN ORDERTIME TIMESTAMP;
ALTER TABLE ORDERS ADD COLUMN PRICESUM INT;

CREATE TABLE ORDERITEMS (ID INT PRIMARY KEY GENERATED ALWAYS AS IDENTITY, ORDERID INT, ITEMNAME VARCHAR(255), QTY INT); 
ALTER TABLE ORDERITEMS ADD FOREIGN KEY (ORDERID) REFERENCES ORDERS(ORDERID);
ALTER TABLE ORDERITEMS ADD FOREIGN KEY (ITEMNAME) REFERENCES ITEMS(ITEMNAME);